# credentials — never commit real WiFi keys
robot/boot/_root.sh.local
*.psk
*.key

# build artifacts
build/
install/
log/
__pycache__/
*.pyc
.colcon_install_layout

# Stock ultralytics checkpoints — not build inputs (build_yolo.sh exports from AI-Hub)
scripts/companion/camera/*.pt

# MiDaS depth artifacts: reproducible via build_depth.sh (--fetch-static-assets); not committed pending runtime integration
scripts/companion/camera/models/midas_depth_*.bin
scripts/companion/camera/models/midas_depth_*_242.dlc
