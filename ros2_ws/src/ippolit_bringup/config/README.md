# Per-node parameter YAML

Populated in Phase A2 (declared ROS parameters replacing env-var configuration). Empty placeholder for
now so this directory exists in git and `install(DIRECTORY config ...)` in CMakeLists.txt has something
to install.
